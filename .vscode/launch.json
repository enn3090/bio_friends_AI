{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 현재 파일 (가상환경)",
            "type": "debugpy",  // "python"에서 "debugpy"로 변경
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/venv/bin/python" 
        }
    ]
}